import React, { Component } from 'react'
import { Text, View } from 'react-native'
import {saveToStorage} from '../utilities/saveFavorite'

export default class Favorite extends Component {
    render() {
        
        return (
            <View>
                <Text> Favorite </Text>
            </View>
        )
    }
}
